name: embassy_patrol
description: Embassy Security Patrol Management System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  supabase_flutter: ^2.0.0
  firebase_core: ^4.9.0
  firebase_messaging: ^16.2.2
  flutter_local_notifications: ^17.1.0
  mobile_scanner: ^3.5.7
  geolocator: ^10.1.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  image_picker: ^1.0.4
  camera: ^0.10.5
  provider: ^6.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image: ^4.1.3
  intl: ^0.18.1
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  record: ^6.2.0
  audioplayers: ^5.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
